import React from "react";

const Test = ({ label, value }) => {
    return (
        <div>
            <div>{label}</div>
            <p />
            <div>{value}</div>
        </div>
    );
};

export default Test;
